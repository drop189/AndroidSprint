<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidSprint" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AndroidSprint" parent="Base.Theme.AndroidSprint">
        <item name="android:statusBarColor">@color/color_surface_main</item>
        <item name="android:navigationBarColor">@color/color_surface_main</item>
    </style>
</resources>